'use client';

import AdminDashboard from '../../AdminDashboard';

export default function AdminDashboardPage() {
  return (
    <AdminDashboard
      articles={[]}
      setArticles={() => {}}
      onClose={() => {}}
    />
  );
}
